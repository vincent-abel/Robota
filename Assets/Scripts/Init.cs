using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Init : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        Terminal MainTerm = new Terminal();


    }

    // Update is called once per frame
    void Update() {

    }
}
